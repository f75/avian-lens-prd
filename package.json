{
  "name": "avian-lens",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:local": "npx vite build",
    "deploy": "npm run build:local && git add dist/ -f && git diff --cached --quiet || git commit -m 'chore: rebuild dist' && git push origin main"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}